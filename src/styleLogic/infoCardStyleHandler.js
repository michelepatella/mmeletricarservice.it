/**
 * Method to handle the style of the info card
 * description dynamically.
 * @returns {{opacity: string, marginTop: string}}
 */
export const handleDescriptionStyle = () => {
	return {
		marginTop: "0",
		opacity: "var(--low_opacity)",
	};
};
